const express = require('express'); const { authenticateAdmin } = require('../../middleware/auth'); const { createRoom, updateRoom, deleteRoom } = require('../../controllers/admin/roomsAdminController'); const router = express.Router(); router.use(authenticateAdmin); router.post('/', createRoom); router.put('/:id', updateRoom); router.delete('/:id', deleteRoom); module.exports = router;